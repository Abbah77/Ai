android {
    ...
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
    // Even if you use Java, Flutter needs this for its own Kotlin parts
    kotlinOptions {
[span_0](start_span)        jvmTarget = '17'[span_0](end_span)
    }
}
