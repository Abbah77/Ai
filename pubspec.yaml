name: here
description: AI Chat Assistant with WebView Integration
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Crucial for the Bridge logic
  webview_flutter: ^4.10.0  # Updated for better JS support
  
  # UI and Data
  shared_preferences: ^2.2.2
  flutter_markdown: ^0.6.11
  
  # Aesthetics
  google_fonts: ^6.1.0
  animations: ^2.0.9
  iconsax_flutter: ^1.0.0
  haptic_feedback: ^0.6.4+3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # Only include assets if the folder actually exists!
  # assets:
  #   - assets/
